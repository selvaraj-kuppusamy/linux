#!/bin/bash

echo "service commnad in linux"
echo "Starting an apache2 server on ubuntu"
service apache2 start

echo "Restaring an apache2 server on ubuntu"
service apache2 restart

echo "Stopping a apache2 server on ubuntu"
service apache2 stop

