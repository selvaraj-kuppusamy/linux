#commands
#list directories or files
ls

#make directory
mkdir

#remove or delete directory
rmdir 
rm -r

#rename files
mv file01.txt file02.txt

#copying one file to another file
cp file01.txt file02.txt

#remove or delete files 
rm file.txt
rm *.txt
rm *

#maximize or minimize the current screen
wmctrl -r ':ACTIVE:' -b toggle,fullscreen

#print standard output
cat file.txt

#print multiple files  standart output
cat file01.txt file02.txt

#append one file to another file 
cat file01.txt>>file.txt

#create files
touch file.txt
cat > file.txt

#date and time 
date

#calendor
cal 
cal 2001
cal year

#change directory 
cd
cd ~
cd /
cs-
cd ..

#present working directory 
pwd

#list harware
lshw

#list harddisk
fdisk -l

#list cpu
lscpu
cat /proc/cpuinfo

#ram used and free
free

#system information
screenfetch

#memory usage
cat /proc/meminfo
vmstat
top
htop

#operating system name 
uname
unmae -a
uname -r
uname -s
uname -v

#access read write and execute
chmod o=rwe,g=rw,o=r file.txt
chmod 766 file.txt
chmod -x file.txt
chmod +x file.txt

#suod privilages
sudo 

#change root user
su -

#list history
history

#permantely delete all history 
history -c

#clear the screen
cltr+l
clear

#your own name to command
alias cls=clear
alias ubuntudetails=screenfetch

#check sorted by files in line by line
comm file01.txt file02.txt
comm -12 file01.txt file02.txt

#check no of bytes in a file
cksum filename

#compare two file byte by byte
cmp file01.txt file02.txt 

#Usage of disk
df

#list the directories
dir

#compare files by line by line
diff -n file01.txt file02.txt

#Estimate file space usage
du

#convert and copy of file
dd if=/dev/sda of=/dev/sdb

#Run a program in modified environment
env 
env -0
env -i /bin/sh

#Display the line by text
echo "I am selvaraj"

#Cause the program termination
exit

#Evaluate Experssions
variable=`expr $variable + 1`
i=`expr $i +1`

#Eject removable media
eject -h
eject -v

#mkdir mutiple directories
mkdir -p selva/selva{1..100}
mkdir -p selva/file{1..2}no{1..10}.odt
mkdir -p selva/c
mkdir -p selva/{linux,windows,unix}

#rmdir mutiple directories
rmdir -p selva/selva{1..100}
rmdir -p selva/file{1..2}no{1..10}.odt
rmdir -p selva/c
rmdir -p selva/{linux,windows,unix}

#prints current day
(date +%A)

#prints prime factors of given number
factor 100
factor 221
factor 10

#search file in the directory hierarchy
find ./Documents -name s.txt
find ./Downloads -nmae -perm 664
find ./c -empty

#Display free sapce of memory and usage of memory
free -h
free

#print the groups
groups
groups selvaraj
groups root

#gzip commnads
gzip  -v selva.txt

#gunzip commands 
gunzip selva.txt.gz


